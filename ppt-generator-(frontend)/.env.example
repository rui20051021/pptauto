VITE_PORT=3000
VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# Optional: if the frontend is deployed separately from the backend,
# set the absolute backend base URL here. Leave empty during local dev
# and Vite will proxy /api requests to VITE_API_PROXY_TARGET.
VITE_API_BASE_URL=
